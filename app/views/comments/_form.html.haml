= form_for([@article, @article.comments.build]) do |f|
  %p
    = f.label :body, t("comments.form.text")
    %br/
    = f.text_area :body, class: 'form-control', rows: '2'
  %p
    = f.submit t("buttons.create_c"), class: 'btn btn-default'
