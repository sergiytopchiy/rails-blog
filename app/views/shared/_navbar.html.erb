<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">BlogSerega</a>
    </div>
    <ul class="nav navbar-nav">
      <li class="<%= navbar_link('articles') %>">
        <a href="<%= articles_path %>">Articles</a>
      </li>
    </ul>
    <% if user_signed_in? %>
      <div class="nav navbar-nav navbar-right">
        <a class="btn btn-default navbar-btn" href="<%= destroy_user_session_path %>">Sign out</a>
      </div>
      <div class="nav navbar-nav navbar-right">
        <a class="btn btn-default navbar-btn" href="<%= edit_user_registration_path %>">User cabinet</a>
      </div>
      <div class="nav navbar-nav navbar-right">
        <p class="navbar-text">Signed in as <%= current_user.name %></p>
      </div>

    <% end %>
  </div>
</nav>
