= form_for @article do |f|
  - if @article.errors.any?
    #error_explanation
      %h2
        = pluralize(@article.errors.count, "error")
        prohibited
        this article from being saved:
      %ul
        - @article.errors.full_messages.each do |msg|
          %li= msg
  %p
    = f.label :title, t("articles.form.title")
    %br/
    = f.text_field :title, class: 'form-control'
  %p
    = f.label :text, t("articles.form.text")
    %br/
    = f.text_area :text, class: 'tinymce form-control', rows: '40', colls: '120'
  %p
    = f.submit button_text, class: 'btn btn-default'
  -# - content_for :categories do
  -#   %ul
  -#     - Category.all.each do |category|
  -#       %li
  -#         = check_box_tag "#{category.category}", category.id, checked(category)
  -#         = label_tag("#{category.category}")
